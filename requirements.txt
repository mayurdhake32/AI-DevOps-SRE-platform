# ============================================
# AI DevOps SRE - Render-Safe Requirements
# Python 3.11 Compatible | No Rust/C++ Compile
# ============================================

# --- Core Web Framework ---
fastapi>=0.103.0,<0.111.0
uvicorn[standard]>=0.23.0,<0.28.0
python-multipart>=0.0.6

# --- Frontend ---
streamlit>=1.28.0,<1.40.0
pandas>=2.0.0,<2.3.0

# --- ML / Data (Pre-built wheels only) ---
scikit-learn>=1.3.0,<1.5.0
joblib>=1.3.0
numpy>=1.24.0,<1.27.0

# --- Pydantic v1 (NO Rust compilation!) ---
pydantic<2,>=1.10.0

# --- RAG / Vector DB ---
# ChromaDB can be heavy; use a lighter pinned version
chromadb>=0.4.0,<0.5.0
sentence-transformers>=2.2.0,<2.6.0

# --- LangChain (Compatible versions) ---
langchain>=0.0.350,<0.2.0
langchain-community>=0.0.1,<0.1.0
langchain-huggingface>=0.0.1

# --- LLM ---
openai>=1.0.0,<1.15.0

# --- Config & Utils ---
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0

# --- Logging ---
# structlog optional - remove if causing issues
